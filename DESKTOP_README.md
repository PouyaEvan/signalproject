# 🧠 Brain Signal Analyzer - Desktop Application

اپلیکیشن دسکتاپ Python برای تحلیل سیگنال مغزی، فیلترینگ، حذف آرتیفکت و تشخیص احساس.

## ✨ امکانات

- 📊 **سیگنال‌های از پیش تعریف شده**: خوشحال، خنثی، ناراحت
- 🎛️ **طراحی سیگنال سفارشی**: تنظیم امواج آلفا، بتا، تتا، دلتا و گاما
- 🔬 **فیلترهای پردازش سیگنال**:
  - Band-Pass Filter (0.5-45 Hz)
  - Notch Filter (50 Hz)
  - حذف آرتیفکت (ICA)
- 📈 **تحلیل فرکانسی**: نمایش طیف و قدرت باندهای مختلف
- 🎭 **تشخیص احساس**: طبقه‌بندی بر اساس قدرت باندهای فرکانسی
- 🎵 **پیشنهاد موسیقی**: جستجوی آهنگ متناسب با احساس تشخیص داده شده

## 📋 پیش‌نیازها

Python 3.8 یا بالاتر

## 🚀 نصب و اجرا

### روش 1: اجرای مستقیم Python

```bash
# نصب پکیج‌ها
pip install customtkinter numpy scipy matplotlib pillow requests

# اجرای برنامه
python brain_signal_app.py
```

### روش 2: ساخت فایل EXE (یکپارچه)

```bash
# نصب PyInstaller
pip install pyinstaller

# ساخت فایل exe تک‌فایلی
pyinstaller --onefile --windowed --name="BrainSignalAnalyzer" brain_signal_app.py
```

فایل exe در پوشه `dist` ساخته می‌شود.

### روش پیشرفته ساخت EXE (با آیکون سفارشی)

```bash
pyinstaller --onefile --windowed --icon=brain.ico --name="BrainSignalAnalyzer" --add-data "customtkinter;customtkinter/" brain_signal_app.py
```

## 🎯 راهنمای استفاده

### 1. بارگذاری سیگنال
از پنل سمت چپ یکی از سیگنال‌های زیر را انتخاب کنید:
- 😊 **خوشحال**: امواج آلفا و گاما قوی
- 😐 **خنثی**: تعادل بین امواج
- 😢 **ناراحت**: امواج تتا و دلتا قوی
- 🎛️ **سفارشی**: تنظیم دستی هر موج

سپس دکمه **"📥 بارگذاری سیگنال"** را بزنید.

### 2. پردازش سیگنال
فیلترهای مورد نظر را انتخاب کنید:
- **Band-Pass**: حذف فرکانس‌های خارج از 0.5-45 Hz
- **Notch Filter**: حذف نویز برق 50 Hz
- **حذف آرتیفکت (ICA)**: حذف آرتیفکت‌های چشم و حرکت

دکمه **"🔬 پردازش و تحلیل"** را بزنید.

### 3. مشاهده نتایج
- **تب سیگنال**: مقایسه سیگنال اصلی و پردازش‌شده
- **تب فرکانس**: طیف فرکانسی و قدرت باندها
- **تب احساس**: نتیجه تشخیص احساس با درصد اطمینان

### 4. پیشنهاد موسیقی
1. API Key اسپاتیفای را در پنل سمت چپ وارد کنید
2. به تب **"🎵 موسیقی"** بروید
3. ژانر مورد نظر را انتخاب کنید
4. دکمه **"🔍 جستجوی موسیقی"** را بزنید

## 📦 ساختار باندهای فرکانسی

| باند | فرکانس (Hz) | ارتباط با احساس |
|------|-------------|-----------------|
| Delta (δ) | 0.5-4 | خواب عمیق، ناراحتی |
| Theta (θ) | 4-8 | خواب‌آلودگی، ناراحتی |
| Alpha (α) | 8-13 | آرامش، خوشحالی |
| Beta (β) | 13-30 | تمرکز، فعالیت |
| Gamma (γ) | 30-45 | پردازش شناختی، خوشحالی |

## 🔧 عیب‌یابی

### خطا در ساخت exe
اگر exe ساخته شده کار نکرد:
```bash
# نصب تمام پکیج‌ها با نسخه مشخص
pip install customtkinter==5.2.1 numpy==1.26.0 scipy==1.11.3 matplotlib==3.8.0 pillow==10.1.0 requests==2.31.0

# ساخت مجدد
pyinstaller --onefile --windowed --collect-all customtkinter --name="BrainSignalAnalyzer" brain_signal_app.py
```

### مشکل در نمایش UI
اگر UI درست نمایش داده نشد:
```bash
pip uninstall customtkinter
pip install customtkinter
```

## 📄 مجوز

این پروژه برای اهداف آموزشی ایجاد شده است.

## 👨‍💻 توسعه‌دهنده

ساخته شده با ❤️ برای پروژه سیگنال
