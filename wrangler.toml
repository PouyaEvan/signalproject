name = "signalproject"
compatibility_date = "2025-12-14"

# Deploy static assets generated by `next export` (output in ./out)
assets = { directory = "./out" }

# Optional: custom domain/project can be set via env vars in CI
# account_id and project_name are supplied by the GitHub Action secrets.
